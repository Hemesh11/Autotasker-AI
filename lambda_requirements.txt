# Lambda-specific requirements (EXCLUDE UI dependencies)
# DO NOT include: streamlit, plotly, or other UI frameworks

# Core LLM and Agent Framework
langchain>=0.1.0
langgraph>=0.0.60
langchain-openai>=0.0.5
openai>=1.12.0

# AWS Integration
boto3>=1.34.0
botocore>=1.34.0

# Google Calendar Integration
google-api-python-client>=2.100.0
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.1.1

# Date/Time Handling
pytz>=2023.3
python-dateutil>=2.8.2

# Configuration
python-dotenv>=1.0.0
pyyaml>=6.0

# HTTP Requests
requests>=2.31.0

# Email (if using SES)
# Already included in boto3

# Optional: If you need caching in Lambda
redis>=5.0.0

# Optional: If using PostgreSQL with Lambda
# psycopg2-binary>=2.9.9
